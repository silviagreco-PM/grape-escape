[build]
  publish = "deploy"
  command = ""

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_URL"

[functions]
  directory = "netlify/functions"

[functions."daily-push"]
  schedule = "0 6 * * *"
